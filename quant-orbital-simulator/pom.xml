<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://apache.org"
         xmlns:xsi="http://w3.org"
         xsi:schemaLocation="http://apache.org http://apache.org">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.quantorbital</groupId>
        <artifactId>quant-orbital-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>quant-orbital-simulator</artifactId>

    <dependencies>
        <dependency>
            <groupId>com.quantorbital</groupId>
            <artifactId>quant-orbital-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.quantorbital</groupId>
            <artifactId>quant-orbital-mesh</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>com.quantorbital.simulator.EngineSimulatorApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
